#include <bits/stdc++.h>
using namespace std;

string stringWindow(string big, string small)
{
}

int main()
{
    string big, small;
    cin >> big >> small;
    cout << stringWindow(big, small);
    return 0;
}