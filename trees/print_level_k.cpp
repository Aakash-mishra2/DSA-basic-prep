#include <bits/stdc++.h>
using namespace std;

class node
{
};

node *buildTree()
{
}

void printAtLevelK(node *root, int k)
{
}

int main()
{
    freopen("treeinput.txt", "r", stdin);
    node *root = buildTree();
    int K = 2;
    printAtLevelK(root, K);
    return 0;
}